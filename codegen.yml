overwrite: true
schema:
  - http://localhost:8080/graphql/a3333333-b111-c111-d111-e00000000022:
      headers:
        Authorization: `Bearer ${token}`
# documents: "src/**/*.graphql"
generates:
  src/generated/graphql.tsx:
    plugins:
      - "typescript"
      # - "typescript-operations"
      # - "typescript-react-apollo"
